inherit gnome2

DESCRIPTION="GNOME desktop compositing window manager"

PATCH_URI="3.0.0-no-undefined.patch"

PKG_NAMES="${PN} ${PN}-devel"
PKG_HINTS="setup devel"
mutter_CONTENTS="etc/ usr/bin/ usr/lib/mutter/ usr/share/"
mutter_devel_CONTENTS="usr/include/ usr/lib/lib* usr/lib/pkgconfig/"
